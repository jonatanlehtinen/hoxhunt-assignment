# HoxHunt Summer Hunters 2021 - Home assignment

## Running the assignment

In order to run the notebook, first install the required packages:

```
python3 -m pip install -r requirements.txt
jupyter notebook
```